from adafruit_circuitplayground import cp
import time

while True:
    cp.pixels.fill((255,0,0))
    cp.play_tone(400, .500)
    time.sleep(0.500)
    cp.pixels.fill((0,0,255))
    cp.play_tone(900, .500)
    time.sleep(0.500)
